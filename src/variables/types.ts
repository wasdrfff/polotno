export type Theme = {
  colors: {
    focusColor: string;
    activeColor: string;
    textColor: string;
    textColor2: string;
    systemRed: string;
    systemYellow: string;
    systemGreen: string;
    mainColor: string;
    white: string;
    buttonColor: string;
  };

  sizes: {
    mobile: string;
    tablet: string;
    desktop: string;
  };
};
